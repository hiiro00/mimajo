<%= content_for(:body_index, "index") %>
<span id="disp_index_selectroom">

<span class="notice"><%= notice %></span>
<span class="alert"><%= alert %></span>

<% if user_signed_in? %>
  ログインしました：<strong><%= current_user.name %></strong>　
  <%= link_to "Logout", destroy_user_session_path, method: :delete %>
<% end %>

<% logger.debug("■current_user print") %>
<% logger.debug(current_user.email) %>


<div class="outer" ontouchstart="">
    <div class="out_relative">

      <div class="title_small">
        <img src="/logo_data_01.jpg">
      </div><br>
      <div class="kadomaru inner">
          部屋新規作成
          <%= link_to '', rooms_path(email: current_user.email , name: current_user.name ,position:'owner') , method: :post %>  
      </div><br>

      <div class="kadomaru inner">
          部屋一覧へ
          <%= link_to '', rooms_path %>
      </div><br>
      
      <div class="kadomaru inner">
          戻る
          <%= link_to '', controller: :welcome %>
      </div><br>
  </div>
</div>

